FROM sudo248dev/sudoo

WORKDIR /app

ENV JAR_FILE=order-service-0.0.1.jar
ENV APP=/app/$JAR_FILE
ENV PORT=8085
ENV ENABLE_STAFF_FULL_CONTROL_ORDER_STATUS=true

COPY target/$JAR_FILE $APP
EXPOSE $PORT

CMD /app/start-app.sh